pydantic>=2.5,<3.0
requests>=2.31,<3.0
# schema.py uses `X | None` annotations; pydantic evaluates them on 3.9 only via this shim
eval_type_backport>=0.2,<1; python_version < "3.10"

# Testing
pytest>=7.4,<9.0
pytest-playwright>=0.4,<1.0
playwright>=1.40,<2.0
